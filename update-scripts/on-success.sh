#!/bin/bash -e

echo "This script runs after the update is successful and has beeen committed"

# Cleanup
# [ -d /tmp/new-dir-tree ] && rm -rf /tmp/new-dir-tree
# [ -d /tmp/prev-dir-tree ] && rm -rf /tmp/prev-dir-tree